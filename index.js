// index.js
import { extension_settings, getContext } from "../../../extensions.js";

const MODULE_NAME = "st-timeline-memory";

jQuery(async () => {
    console.log(`[${MODULE_NAME}] 插件已加载`);
    // 待会我们往这里加树状图面板、总结逻辑、跳转逻辑
});
